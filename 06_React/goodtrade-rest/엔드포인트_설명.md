# 🔌 엔드포인트란? 누가 어디서 누구한테 요청하는가?

## 📍 엔드포인트의 기본 개념

**엔드포인트(Endpoint)**는 클라이언트(프론트엔드)가 서버(백엔드)에게 요청을 보내는 **주소**입니다.

```
프론트엔드 (클라이언트)  →  백엔드 (서버)
     요청 보냄              요청 받아서 처리
```

---

## 🎯 구체적인 예시

### 예시 1: 상품 등록하기

#### 1️⃣ 프론트엔드에서 요청 보내기

**위치:** `frontend/src/context/ProductContext.jsx`

```javascript
const addProduct = async (newProduct) => {
  // 여기서 백엔드로 요청을 보냅니다!
  const response = await axios.post(
    `/api/products?sellerId=${newProduct.seller}`,  // ← 이것이 엔드포인트!
    productData
  );
};
```

**누가?** 프론트엔드 (React 컴포넌트)  
**어디서?** `ProductContext.jsx` 파일의 `addProduct` 함수  
**누구한테?** 백엔드 서버 (`http://localhost:8080`)  
**무엇을?** 상품 데이터를 등록해달라는 요청

---

#### 2️⃣ 백엔드에서 요청 받기

**위치:** `backend/src/main/java/com/kh/server/controller/ProductController.java`

```java
@RestController
@RequestMapping("/api/products")
public class ProductController {

    @PostMapping  // ← 이 엔드포인트가 요청을 받습니다!
    public ResponseEntity<?> create(
        @RequestBody ProductRequestDto dto, 
        @RequestParam Long sellerId
    ) {
        // 프론트엔드에서 보낸 요청을 여기서 처리합니다
        ProductResponseDto responseDto = productService.create(dto, sellerId);
        return ResponseEntity.status(HttpStatus.CREATED).body(responseDto);
    }
}
```

**누가?** 백엔드 (Spring Boot 서버)  
**어디서?** `ProductController.java` 파일의 `create` 메서드  
**누구한테?** 프론트엔드에게 응답을 보냄  
**무엇을?** 상품을 데이터베이스에 저장하고 결과를 반환

---

## 🔄 전체 흐름도

```
┌─────────────────────────────────────────────────────────┐
│  프론트엔드 (React)                                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ProductRegister.jsx                              │   │
│  │ 사용자가 "등록하기" 버튼 클릭                    │   │
│  └──────────────┬───────────────────────────────────┘   │
│                 │ handleSubmit() 호출                    │
│                 ↓                                        │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ProductContext.jsx                              │   │
│  │ addProduct() 함수 실행                          │   │
│  │                                                  │   │
│  │ axios.post('/api/products?sellerId=1', data)    │   │
│  │         ↑                                        │   │
│  │         └─ 이게 엔드포인트!                     │   │
│  └──────────────┬───────────────────────────────────┘   │
└─────────────────┼───────────────────────────────────────┘
                  │ HTTP 요청 (POST)
                  │ URL: /api/products?sellerId=1
                  │ Body: { name, price, category, ... }
                  ↓
┌─────────────────┼───────────────────────────────────────┐
│  Vite Proxy (프록시)                                     │
│  vite.config.js의 proxy 설정이                          │
│  /api 요청을 백엔드로 전달                              │
└─────────────────┼───────────────────────────────────────┘
                  │
                  ↓
┌─────────────────┼───────────────────────────────────────┐
│  백엔드 (Spring Boot)                                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ProductController.java                          │   │
│  │                                                  │   │
│  │ @PostMapping("/api/products")                   │   │
│  │ public ResponseEntity<?> create(...) {          │   │
│  │     ↑                                            │   │
│  │     └─ 이 엔드포인트가 요청을 받습니다!         │   │
│  │                                                  │   │
│  │     // 요청 처리                                 │   │
│  │     ProductService.create() 호출                │   │
│  │     데이터베이스에 저장                         │   │
│  │                                                  │   │
│  │     return ResponseEntity.ok(responseDto);      │   │
│  │     ↑                                            │   │
│  │     └─ 응답을 프론트엔드로 보냅니다             │   │
│  └──────────────┬───────────────────────────────────┘   │
└─────────────────┼───────────────────────────────────────┘
                  │ HTTP 응답 (200 OK)
                  │ Body: { id, name, price, ... }
                  ↓
┌─────────────────┼───────────────────────────────────────┐
│  프론트엔드 (React)                                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ProductContext.jsx                              │   │
│  │ response를 받아서 처리                          │   │
│  │ loadProducts()로 목록 새로고침                  │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  화면에 새로운 상품이 표시됨!                            │
└──────────────────────────────────────────────────────────┘
```

---

## 📝 엔드포인트 구성 요소

### 1. HTTP 메서드
- **GET**: 데이터 조회 (읽기)
- **POST**: 데이터 생성 (쓰기)
- **PUT**: 데이터 수정
- **DELETE**: 데이터 삭제

### 2. URL 경로
- 예: `/api/products`
- 예: `/api/users/login`
- 예: `/api/products/1` (1은 상품 ID)

### 3. 쿼리 파라미터 (선택사항)
- 예: `/api/products?sellerId=1`
- 예: `/api/products/search?keyword=맥북`

---

## 🎯 실제 코드 예시

### 프론트엔드 → 백엔드 요청

**파일:** `frontend/src/context/ProductContext.jsx`

```javascript
// 상품 등록
const response = await axios.post(
  '/api/products?sellerId=1',  // ← 엔드포인트
  {
    name: "맥북 프로",
    price: 2500000,
    category: "전자기기"
  }
);
```

### 백엔드에서 받기

**파일:** `backend/src/main/java/com/kh/server/controller/ProductController.java`

```java
@PostMapping  // ← 이게 엔드포인트!
public ResponseEntity<?> create(
    @RequestBody ProductRequestDto dto,
    @RequestParam Long sellerId
) {
    // 요청 처리
    return ResponseEntity.ok(result);
}
```

---

## 💡 핵심 정리

### 엔드포인트는...

1. **프론트엔드가 백엔드에게 요청을 보내는 주소**
   - 예: `axios.post('/api/products', data)`

2. **백엔드가 요청을 받는 곳**
   - 예: `@PostMapping("/api/products")`

3. **두 개가 서로 맞아야 함!**
   - 프론트: `/api/products` → 백엔드: `/api/products` ✅
   - 프론트: `/api/products` → 백엔드: `/api/product` ❌ (에러!)

---

## 🔍 다른 예시들

### 예시 2: 로그인

**프론트엔드:**
```javascript
// Login.jsx → UserContext.jsx
axios.post('/api/users/login', { id, pwd })
```

**백엔드:**
```java
// MemberController.java
@PostMapping("/login")  // ← 엔드포인트
public ResponseEntity<?> login(...) { ... }
```

### 예시 3: 상품 조회

**프론트엔드:**
```javascript
// ProductContext.jsx
axios.get('/api/products')
```

**백엔드:**
```java
// ProductController.java
@GetMapping  // ← 엔드포인트
public ResponseEntity<List<ProductResponseDto>> getAll() { ... }
```

---

## 🎓 요약

| 항목 | 설명 |
|------|------|
| **누가?** | 프론트엔드 (React 컴포넌트) |
| **어디서?** | Context 파일 (UserContext.jsx, ProductContext.jsx) |
| **누구한테?** | 백엔드 서버 (Spring Boot) |
| **어떻게?** | HTTP 요청 (GET, POST, PUT, DELETE) |
| **무엇을?** | 데이터 조회, 생성, 수정, 삭제 요청 |
| **어디서 받나?** | Controller 파일 (MemberController.java, ProductController.java) |

---

**엔드포인트 = 프론트엔드와 백엔드가 소통하는 대화의 주소!** 📡

